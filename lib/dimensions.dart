import 'package:get/get.dart';

class Dimensions {
  static double sH = Get.context!.height;
  static double sW = Get.context!.width;
  static double dH = 844; //Default Device Height;

  static double size10 = sH / (dH / 10); //Default Device Height;
}
